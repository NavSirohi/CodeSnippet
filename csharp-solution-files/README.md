# C# Solutions

Each `task-XX.cs` file is a self-contained console program. Compile and run one file at a time, for example with the .NET SDK:

```bash
dotnet new console -n Task01
cp task-01.cs Task01/Program.cs
dotnet run --project Task01
```
